{
  "scraper_mds": [
    {
      "name": "FL DEFAULT",
      "md": "[page][goto] https://www.myfloridacounty.com/official_records/index.html?thisPage=MyFloridaCounty.ORI.Order.state.Start\n[page][waitforselector] #countyDropdown\n[page][evaluate] (function(){ var sel = document.getElementById('countyDropdown'); var opt = Array.from(sel.options).find(function(o){ return o.text.trim().toUpperCase() === '${query.county}'.toUpperCase(); }); if(!opt) throw new Error('County not found: ${query.county}'); sel.value = opt.value; sel.dispatchEvent(new Event('change', {bubbles:true})); return true; })() [text=\"Select County\"]\n[page][evaluate] (function(){ var btn = document.querySelector('button[onclick*=\"redirectToCounty\"]'); if(!btn) throw new Error('Go button not found'); btn.click(); return true; })() [text=\"Redirect to the selected County website\"]\n[page][waitforurl] /orisearch/ [text=\"Waiting for county search page\"]\n[if query.documentType==='deed']\n    [page][waitforselector] #documentTypeID\n    [page][evaluate] (function(){ var sel = document.getElementById('documentTypeID'); var opt = Array.from(sel.options).find(function(o){ return o.text.trim().toUpperCase() === '${query.documentType}'.toUpperCase(); }); if(!opt) throw new Error('Document type not found: ${query.documentType}'); Array.from(sel.options).forEach(function(o){ o.selected = false; }); opt.selected = true; sel.dispatchEvent(new Event('change', {bubbles:true})); return true; })() [text=\"Select document type: ${query.documentType}\"]\n[endif]\n\n[if query.formType==='name']\n    [page][waitforselector] #name\n    [if query.isBusinessName==='true']\n        [page][do] await page.type('#name', \"${query.ownerLastName}\") [text=\"Type the business name in the Owner Name field ${query.ownerLastName}\"]\n    [else]\n        [page][do] await page.type('#name', \"${query.ownerLastName} ${query.ownerFirstName}*\") [text=\"Type the owner name in the Owner Name field ${query.ownerLastName} ${query.ownerFirstName}*\"]\n    [endif]\n    [page][waitfor] 2000\n    [page][waitforselector] input[value=\"Search\"]\n    [page][do] await page.click('input[value=\"Search\"]') [text=\"Click the Search button\"]\n    [page][waitfor] 4000 [text=\"Wait for search results to load\"]\n    [page][captcha-detection] [text=\"Check if captcha is present after search\"]\n    [page][waitfor] 1000\n    [page][evaluate] (function(){ var err = document.querySelector('span.error'); if(err && err.textContent.toLowerCase().includes('no records')) throw new Error('NO_RESULTS: No results found'); return true; })() [text=\"Check if results found\"]\n    [page][waitforselector] #ori_results\n    [page][smart-download] ./downloads/${query.propertyId}/deed.pdf 300000 [text=\"Smart download FL\"]\n\n[elseif query.formType==='book_page']\n    [page][waitforselector] #name\n    [page][evaluate] (function(){ var el = document.querySelector('#name'); if(el){ el.value = ''; el.dispatchEvent(new Event('input', {bubbles:true})); } return true; })() [text=\"Clear the Owner Name field\"]\n    [page][waitfor] 1000\n    [page][waitforselector] #book\n    [page][do] await page.type('#book', \"${query.book}\") [text=\"Type the book number in the Book field ${query.book}\"]\n    [page][waitforselector] #page_number\n    [page][do] await page.type('#page_number', \"${query.page}\") [text=\"Type the page number in the Page field ${query.page}\"]\n    [page][waitfor] 2000\n    [page][waitforselector] input[value=\"Search\"]\n    [page][do] await page.click('input[value=\"Search\"]') [text=\"Click the Search button\"]\n    [page][waitfor] 4000 [text=\"Wait for search results to load\"]\n    [page][captcha-detection] [text=\"Check if captcha is present after search\"]\n    [page][waitfor] 1000\n    [page][evaluate] (function(){ var err = document.querySelector('span.error'); if(err && err.textContent.toLowerCase().includes('no records')) throw new Error('NO_RESULTS: No results found'); return true; })() [text=\"Check if results found\"]\n    [page][waitforselector] #ori_results\n    [page][evaluate] (function(){ var rows = document.querySelectorAll('tr'); var row = Array.from(rows).find(function(r){ return r.textContent.includes('${query.book}/${query.page}'); }); if(!row) throw new Error('Row not found for ${query.book}/${query.page}'); var link = row.querySelector('a.a_btn'); if(!link) throw new Error('View Image not found'); window.__samHref = link.getAttribute('href'); return true; })() [text=\"Find View Image link for ${query.book}/${query.page}\"]\n    [page][download-href] __samhref -> ./downloads/${query.propertyId}/deed.pdf [text=\"Download deed PDF for ${query.book}/${query.page}\"]\n[endif]",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "name": "IA BLACK_HAWK",
      "md": "[page][goto] https://ava.fidlar.com/IABlackHawk/AvaWeb/#/search\n[page][waitfor] 3000\n[if query.isBusinessName==='true']\n[page][do] await page.type('input[formcontrolname=\"LastBusinessName\"]', \"${query.ownerLastName}\")\n[else]\n[page][do] await page.type('input[formcontrolname=\"LastBusinessName\"]', \"${query.ownerLastName}\")\n[page][do] await page.type('input[formcontrolname=\"FirstName\"]', \"${query.ownerFirstName}\")\n[endif]\n[page][waitfor] 3000\n[page][do] await page.type('input[formcontrolname=\"Book\"]', \"${query.book}\")\n[page][do] await page.type('input[formcontrolname=\"Page\"]', \"${query.page}\")\n[page][waitfor] 3000\n[page][do] await page.click('button[form=\"searchForm\"]')\n[page][waitfor] 3000\n[page][waitforselector] button.yellow\n[page][evaluate] (function(){ var btn = Array.from(document.querySelectorAll('button.yellow')).find(function(b){ return b.textContent.trim().includes('Expand All'); }); if(!btn) throw new Error('Expand All button not found'); btn.click(); return true; })()\n[page][waitfor] 1000\n[page][clickselector] .resultRow:first-child .resultRowDetail button\n[page][waitfor] 5000\n[page][evaluate] (function(){ var el = document.querySelector('button[title=\"Print\"]'); if(!el) throw new Error('Print button not found'); el.click(); return true; })()\n[page][waitfor] 3000\n[page][evaluate] (function(){ var el = Array.from(document.querySelectorAll('button')).find(function(b){ return b.textContent.trim() === 'OK'; }); if(!el) throw new Error('OK button not found'); el.click(); return true; })()\n[stagehand][handoff] Enter CC info to download PDF",
      "config": {
        "_comment": "Configuration for BLACK_HAWK.md dataset",
        "noCaptchaDetection": true,
        "noCaptchaDetectionReason": "BLACK_HAWK.md does have a captcha v3 which we can bypass"
      }
    },
    {
      "name": "IA DEFAULT",
      "md": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.book}` into Book Number field\n[stagehand][act] type `${query.page}` into Page Number field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[page][waitfor] 5000\n[stagehand][act] click the PDF icon on the right side of the first row in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "name": "NC BUNCOMBE-last-recorded",
      "md": "[page][goto] https://registerofdeeds.buncombecounty.org/External/LandRecords/protected/v4/SrchBookPage.aspx\n[stagehand][act] click Advanced Name tab\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Firm / Surname field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Firm / Surname field\n[stagehand][act] type `${query.ownerFirstName}` into the Given Name field\n[endif]\n[stagehand][act] click `Search (All Matches)` button\n[stagehand][act] in the search results table, find the row where Date Filed is `${query.lastRecorded}` and Type is `DEED`, then click the document/image icon in the Images column of that row\n[stagehand][observe] find `Save Document as PDF`\n[stagehand][waitfordownload]\n[stagehand][act] click `Save Document as PDF`\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {}
    },
    {
      "name": "NC BUNCOMBE",
      "md": "[page][goto] https://registerofdeeds.buncombecounty.org/External/LandRecords/protected/v4/SrchBookPage.aspx\n[stagehand][act] click Book/Page tab\n[stagehand][observe] find Book / Page Search\n[stagehand][act] fill `Book` input field `${query.book}`\n[stagehand][act] fill `Page` input field `${query.page}`\n[stagehand][act] click `Search` button\n[stagehand][observe] the search results appear\n[stagehand][observe] find the clickable icon in the Images column of the first row\n[page][clickselector] input[type=\"image\"][id*=\"ibImage\"]\n[stagehand][observe] find `Save Document as PDF`\n[stagehand][waitfordownload]\n[stagehand][act] click `Save Document as PDF`\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for BUNCOMBE.md dataset"
      }
    },
    {
      "name": "NC DAVIDSON",
      "md": "[page][goto] https://www.davidsondeeds.com/davidsonNameSearch.php\n[stagehand][observe] `LEGAL DISCLAIMER`\n[stagehand][act] click `Accept` button\n[stagehand][act] fill `Book` input field `${query.book}`\n[stagehand][act] fill `Page` input field `${query.page}`\n[stagehand][act] on `Book-Page (OR) File # Search`, click the `Search` button\n[stagehand][observe] Detail Screen\n[stagehand][act] on Perm Indexed, click `PDF` link\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for DAVIDSON.md dataset"
      }
    },
    {
      "name": "NC MECKLENBURG",
      "md": "[page][goto] https://meckrod.manatron.com/\n[stagehand][act] click the `Click here to acknowledge disclaimer` link\n[page][goto] https://meckrod.manatron.com/RealEstate/SearchEntry.aspx\n[stagehand][act] fill `Party Name` input field under `Combined Name Search` radio button `${query.ownerLastName} ${query.ownerFirstName}`\n[stagehand][act] fill `Book` input field `${query.book}`\n[stagehand][act] fill `Page` input field `${query.page}`\n[stagehand][act] click the `Search` button above the form\n[stagehand][observe] the search results appear\n[stagehand][act] find the URL of the `View` link in the first row of the the search results and click it\n[page][goto] https://meckrod.manatron.com/RealEstate/SearchImage.aspx\n[stagehand][catchpdfurl]\n[stagehand][act] select the \"Free Clean copy\" radio button\n[stagehand][act] click the `Get Image Now` button\n[stagehand][observe] the PDF viewer has loaded\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for MECKLENBURG.md dataset"
      }
    },
    {
      "name": "NH STRAFFORD",
      "md": "[page][goto] https://ava.fidlar.com/NHStrafford/AvaWeb/#/search\n[page][waitfor] 3000\n[if query.isBusinessName==='true']\n[page][do] await page.type('input[formcontrolname=\"LastBusinessName\"]', \"${query.ownerLastName}\")\n[else]\n[page][do] await page.type('input[formcontrolname=\"LastBusinessName\"]', \"${query.ownerLastName}\")\n[page][do] await page.type('input[formcontrolname=\"FirstName\"]', \"${query.ownerFirstName}\")\n[endif]\n[page][waitfor] 3000\n[page][do] await page.type('input[formcontrolname=\"Book\"]', \"${query.book}\")\n[page][do] await page.type('input[formcontrolname=\"Page\"]', \"${query.page}\")\n[page][waitfor] 3000\n[page][do] await page.click('button[form=\"searchForm\"]')\n[page][waitfor] 3000\n[page][waitforselector] button.yellow\n[page][evaluate] (function(){ var btn = Array.from(document.querySelectorAll('button.yellow')).find(function(b){ return b.textContent.trim().includes('Expand All'); }); if(!btn) throw new Error('Expand All button not found'); btn.click(); return true; })()\n[page][waitfor] 1000\n[page][clickselector] .resultRow:first-child .resultRowDetail button\n[page][waitfor] 5000\n[page][evaluate] (function(){ var el = document.querySelector('button[title=\"Print\"]'); if(!el) throw new Error('Print button not found'); el.click(); return true; })()\n[page][waitfor] 3000\n[page][evaluate] (function(){ var el = Array.from(document.querySelectorAll('button')).find(function(b){ return b.textContent.trim() === 'OK'; }); if(!el) throw new Error('OK button not found'); el.click(); return true; })()\n[stagehand][handoff] Enter CC info to download PDF",
      "config": {
        "_comment": "Configuration for STRAFFORD.md dataset",
        "noCaptchaDetection": true,
        "noCaptchaDetectionReason": "STRAFFORD.md does have a captcha v3 which we can bypass"
      }
    },
    {
      "name": "NJ UNION-last-recorded",
      "md": "[page][goto] https://clerk.ucnj.org/UCPA/DocIndex?s=name\n[stagehand][observe] find search field\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] check \"Deed\" on Document Types\n[stagehand][act] click Search\n[stagehand][observe] find search results\n[stagehand][act] select row with Recorded Date: `${query.lastRecorded}` then select pdf image\n[stagehand][catchpdfurl]\n[stagehand][observe] the PDF viewer has loaded\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {}
    },
    {
      "name": "NJ UNION",
      "md": "[page][goto] https://clerk.ucnj.org/UCPA/DocIndex?s=name\n[stagehand][observe] find search field\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] check \"Deed\" on Document Types\n[stagehand][act] click Search\n[stagehand][observe] find search results\n[stagehand][act] select row with Book/Page: `${query.book}/${query.page}` then select pdf image\n[stagehand][catchpdfurl]\n[stagehand][observe] the PDF viewer has loaded\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for UNION.md dataset"
      }
    },
    {
      "name": "NV DOUGLAS",
      "md": "[page][goto] https://recorder-search.douglasnv.us/Recording/RecordingSearch?mode=Advanced\n[if query.isBusinessName==='true']\n[page][do] await page.type('#Criteria_Filter_LastName', \"${query.ownerLastName}\")\n[else]\n[page][do] await page.type('#Criteria_Filter_LastName', \"${query.ownerLastName}\")\n[page][do] await page.type('#Criteria_Filter_FirstName', \"${query.ownerFirstName}\")\n[endif]\n[if query.book!=='' && query.page!=='']\n[page][do] await page.type(\"#Criteria_Filter_HistoricNumber\", \"${query.book}-${query.page}\")\n[endif]\n[if query.apn!=='']\n[page][do] await page.type(\"#Criteria_Filter_PropertyId\", \"${query.apn}\")\n[endif]\n[page][evaluate] (function(){ var sel = document.getElementById('Filter_DocumentSubtype'); sel.value = 'DEED'; sel.dispatchEvent(new Event('change', {bubbles:true})); return true; })()\n[page][evaluate] (function(){ var sel = document.getElementById('sort-by-options-search'); sel.value = 'RecordingDate'; sel.dispatchEvent(new Event('change', {bubbles:true})); return true; })()\n[page][do] await page.click('#adv-search-btn')\n[page][waitfor] 3000\n[page][evaluate] (function(){ var el = document.querySelector('.search-result:first-child .recording-link'); if(!el) throw new Error('No recording link found'); el.click(); return true; })()\n[page][waitfor] 2000\n[page][evaluate] (function(){ var el = document.querySelector('.helion-image a'); if(!el) throw new Error('PDF link not found'); el.click(); return true; })()\n[page][waitfor] 1000\n[page][downloadnewtab] ./downloads/${query.propertyId}/deed.pdf",
      "config": {
        "_comment": "Configuration for DOUGLAS.md dataset"
      }
    },
    {
      "name": "NY DEFAULT",
      "md": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][waitfor] 3000\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n"
        }
      }
    },
    {
      "name": "NY DUTCHESS",
      "md": "[page][goto] https://apps.dutchessny.gov/County-Clerk-Document-Search/Search.aspx\n[stagehand][observe] find search form\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the Name (Last First) field\n[else]\n[stagehand][act] type `${query.ownerLastName} ${query.ownerFirstName}` into the Name (Last First) field\n[endif]\n[stagehand][act] type `${query.book}` into the Liber field\n[stagehand][act] type `${query.page}` into the Page field\n[stagehand][act] click Search\n[stagehand][observe] find search results\n[page][waitfor] 3000\n[page][clickselector] a.view-document-image-icon\n[stagehand][observe] the PDF viewer has loaded\n[page][downloadiframesrc] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for DUTCHESS.md dataset"
      }
    },
    {
      "name": "NY TOMPKINS",
      "md": "[page][goto] https://countyfusion3.govos.com/countyweb/loginDisplay.action?countyname=Tompkins\n[page][waitfor] 2000\n[stagehand][observe] find `Login as Guest` or `Login as Public` button\n[stagehand][act] click `Login as Guest` or `Login as Public` button\n[page][waitfor] 5000\n[stagehand][observe] find `Accept` button\n[page][clickinsideframe] bodyframe | input#accept\n[page][waitfor] 2000\n[page][clickselector] #dialog img[src*=\"close.gif\"]\n[stagehand][act] click `Search Public Records` option under `What would you like to do today?`\n[page][waitfor] 5000\n[stagehand][act] uncheck All Document Types Document Types section\n[stagehand][act] check only DEED in the Document Types section\n[stagehand][act] under `Specific Criteria` type `${query.lastFirstName}` into the `Name` field\n[stagehand][act] click `Search` button\n[stagehand][observe] find search results table\n[page][waitfor] 5000\n[stagehand][act] in the search results table, find the row with the most recent Recorded date where the address below it contains `${query.streetAddress}` and Doc Type is `DEED`, then click the Control # link in that row\n[page][waitfor] 2000\n[stagehand][catchpdfurl]\n[stagehand][act] click `Save Image`\n[page][waitfor] 3000\n[stagehand][act] click `Download` button in the dialog\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for TOMPKINS.md dataset"
      }
    },
    {
      "name": "OH DEFAULT",
      "md": "[page][goto] https://countyfusion13.govos.com/countyweb/login.do?countyname=${query.countyName}\n[page][waitfor] 2000\n[stagehand][observe] find `Login as Guest` or `Login as Public` button \n[stagehand][act] click `Login as Guest` or `Login as Public` button\n[page][waitfor] 5000\n[stagehand][observe] find `Accept` button\n[page][clickinsideframe] bodyframe | input#accept\n[page][waitfor] 2000\n[page][clickselector] #dialog img[src*=\"close.gif\"]\n[stagehand][act] click `Search Public Records` option under `What would you like to do today?`\n[page][waitfor] 3000\n[stagehand][act] if there is a modal or dialog visible, click the X button to close it, otherwise do nothing\n[stagehand][act] under `Specific Criteria` type `${query.lastFirstName}` into the `Name` field\n[stagehand][act] click `Search` button\n[stagehand][observe] find search results table\n[page][waitfor] 5000\n[page][clicklinkbyrowtext] ${query.book} && ${query.page} | resultListFrame\n[page][waitfor] 2000\n[stagehand][catchpdfurl]\n[stagehand][act] click `Save Image`\n[page][waitfor] 3000\n[stagehand][act] click `Download` button in the dialog\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "name": "OH FRANKLIN-apn",
      "md": "[page][goto] https://countyfusion5.kofiletech.us/countyweb/loginDisplay.action?countyname=Franklin\n[page][waitfor] 2000\n[stagehand][observe] find `Login as Guest` or `Login as Public` button\n[stagehand][act] click `Login as Guest` or `Login as Public` button\n[page][waitfor] 5000\n[stagehand][observe] find `Accept` button\n[page][clickinsideframe] bodyframe | input#accept\n[page][waitfor] 2000\n[page][clickselector] #dialog img[src*=\"close.gif\"]\n[stagehand][act] click `Search Public Records` option under `What would you like to do today?`\n[page][waitfor] 3000\n[stagehand][observe] find `Specific Criteria` section\n[stagehand][act] under `Specific Criteria` type `${query.lastFirstName}` into the `Name` field\n[stagehand][act] click `Search` button\n[stagehand][observe] find search results table\n[page][waitfor] 5000\n[page][clicklinkbyrowtext] ${query.apn} && DEED | resultListFrame\n[page][waitfor] 2000\n[stagehand][catchpdfurl]\n[stagehand][act] click `Save Image`\n[page][waitfor] 3000\n[stagehand][act] click `Download` button in the dialog\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {}
    },
    {
      "name": "OH FRANKLIN",
      "md": "[page][goto] https://countyfusion5.kofiletech.us/countyweb/loginDisplay.action?countyname=Franklin\n[page][waitfor] 2000\n[stagehand][observe] find `Login as Guest` or `Login as Public` button\n[stagehand][act] click `Login as Guest` or `Login as Public` button\n[page][waitfor] 5000\n[stagehand][observe] find `Accept` button\n[page][clickinsideframe] bodyframe | input#accept\n[page][waitfor] 2000\n[page][clickselector] #dialog img[src*=\"close.gif\"]\n[stagehand][act] click `Search Public Records` option under `What would you like to do today?`\n[page][waitfor] 3000\n[stagehand][observe] find `Specific Criteria` section\n[stagehand][act] under `Specific Criteria` type `${query.lastFirstName}` into the `Name` field\n[stagehand][act] click `Search` button\n[stagehand][observe] find search results table\n[page][waitfor] 5000\n[page][clicklinkbyrowtext] ${query.book} && ${query.page} | resultListFrame\n[page][waitfor] 2000\n[stagehand][catchpdfurl]\n[stagehand][act] click `Save Image`\n[page][waitfor] 3000\n[stagehand][act] click `Download` button in the dialog\n[stagehand][downloadcaughtpdf] ./downloads/${query.propertyId}/deed.pdf\n",
      "config": {
        "_comment": "Configuration for FRANKLIN.md dataset"
      }
    },
    {
      "name": "TST TEST",
      "md": "[page][goto] https://account.proton.me/mail\n[stagehand][handoff] Please log in to the form. Click Resume when you are logged in.\n[page][goto] https://mail.proton.me/u/1/starred\n[stagehand][act] click dropdown menu in top right where its current email is visible, then click Sign Out",
      "config": {}
    }
  ],
  "scraper_configs": [
    {
      "state_code": "FL",
      "county_name": "BRADFORD",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "CALHOUN",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "DESOTO",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "DIXIE",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "FRANKLIN",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "GILCHRIST",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "GLADES",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "GULF",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "HARDEE",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "HENDRY",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "HOLMES",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "JACKSON",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "LAFAYETTE",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "LIBERTY",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "MADISON",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "NASSAU",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "PUTNAM",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "SUMTER",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "SUWANNEE",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "FL",
      "county_name": "WASHINGTON",
      "scraper_md_name": "FL DEFAULT",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "smartDownload": {
          "label": "Florida Official Records",
          "extract": "fl",
          "columns": [
            {
              "key": "partyName",
              "label": "Party Name",
              "style": "font-weight:500;"
            },
            {
              "key": "partyType",
              "label": "Type",
              "badge": true
            },
            {
              "key": "date",
              "label": "Date",
              "style": "color:#666;"
            },
            {
              "key": "bookPage",
              "label": "Book/Page",
              "style": "font-family:monospace;font-size:12px;"
            },
            {
              "key": "description",
              "label": "Description",
              "style": "color:#555;font-size:12px;"
            }
          ]
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BLACK HAWK",
      "scraper_md_name": "IA BLACK_HAWK",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for BLACK_HAWK.md dataset",
        "noCaptchaDetection": true,
        "noCaptchaDetectionReason": "BLACK_HAWK.md does have a captcha v3 which we can bypass"
      }
    },
    {
      "state_code": "IA",
      "county_name": "ADAIR",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "ADAMS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "ALLAMAAKE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "APPANOOSE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "AUDOBON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BENTON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BLACK_HAWK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BOONE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BREMER",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BUCHANAN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BUENA_VISTA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "BUTLER",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CALHOUN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CARROL",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CASS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CEDAR",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CERRO_GORDO",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CHEROKEE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CHICKASAW",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CLARKE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CLAY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CLAYTON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CLINTON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "CRAWFORD",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DALLAS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DAVIS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DECATUR",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DELAWARE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DES_MOINES",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DICKINSON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "DUBUQUE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "EMMET",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "FAYETTE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "FLOYD",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "FRANKLIN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "FREMONT",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "GREENE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "GRUNDY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "GUTHRIE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HAMILTON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HANCOK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HARDIN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HARRISON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HENRY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HOWARD",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "HUMBOLDT",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "IDA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "IOWA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "JACKSON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "JASPER",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "JEFFERSON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "JOHNSON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "JONE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "KEUKOK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "KOSSUTH",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "LEE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "LINN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "LOUISA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "LUCAS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "LYON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MADISON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MAHASKA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MARION",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MARSHALL",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MILLS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MITCHELL",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MONONA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MONROE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MONTGOMERY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "MUSCATINE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "OBRIEN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "OSCEOLA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "PAGE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "PALO_ALTO",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "PLYMOUTH",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "POCAHONTAS",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "POLK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "POTTAWATTAMIE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "POWESHIEK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "RINGGOLD",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "SAC",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "SCOTT",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "SHELBY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "SIOUX",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "STORY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "TAMA",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "TAYLOR",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "UNION",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "VAN_BUREN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WAPELLO",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WARREN",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WASHINGTON",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WAYNE",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WEBSTER",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WINNEBAGO",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WINNESHIEK",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WOODBURY",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WORTH",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "IA",
      "county_name": "WRIGHT",
      "scraper_md_name": "IA DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.block}` into Block field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the most recent row in the search results sorted by Recorded Date\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot_subdivision": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.subdivision}` into Subdivision field\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] in the search results, find the row with the most recent Recorded Date and click its PDF icon\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "lot": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.lot}` into Lot field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon on the right side of the row with the most recent Recorded Date in the search results\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n",
          "township": "[page][goto] https://iowalandrecords.org/cas/login\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_USERNAME}` into the username field\n[stagehand][act] type `${process.env.CREDENTIALS_IA_HAMILTON_PASSWORD}` into the password field\n[stagehand][act] click the login button\n[stagehand][observe] find Advanced Search link under Start a New Search\n[page][goto] https://iowalandrecords.org/search/guided/advanced?documentType=5&documentType=7&documentType=16&documentType=26&dateRange=NONE\n[stagehand][observe] find the Advanced Search form\n[if query.isBusinessName==='true']\n[stagehand][act] type `${query.ownerLastName}` into the Organization Name field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the Last Name field\n[stagehand][act] type `${query.ownerFirstName}` into the First Name field\n[endif]\n[stagehand][act] type `${query.township}` into Township field\n[stagehand][act] type `${query.county}` into Counties field then select it from the dropdown list\n[page][waitfor] 3000\n[page][press] Enter\n[stagehand][act] Click the `Search` button at the bottom of the Advanced Search form\n[stagehand][observe] find search results\n[stagehand][act] click the PDF icon of the most recent Recorded Date row\n[stagehand][switchtonewpage]\n[stagehand][clickdownload] ./downloads/${query.propertyId}/deed.pdf\n[page][waitfor] 5000\n[page][goto] https://iowalandrecords.org/search/logout\n"
        }
      }
    },
    {
      "state_code": "NC",
      "county_name": "BUNCOMBE-last-recorded",
      "scraper_md_name": "NC BUNCOMBE-last-recorded",
      "scraper_type": "bot-human",
      "config": {}
    },
    {
      "state_code": "NC",
      "county_name": "BUNCOMBE",
      "scraper_md_name": "NC BUNCOMBE",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for BUNCOMBE.md dataset"
      }
    },
    {
      "state_code": "NC",
      "county_name": "DAVIDSON",
      "scraper_md_name": "NC DAVIDSON",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DAVIDSON.md dataset"
      }
    },
    {
      "state_code": "NC",
      "county_name": "MECKLENBURG",
      "scraper_md_name": "NC MECKLENBURG",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for MECKLENBURG.md dataset"
      }
    },
    {
      "state_code": "NH",
      "county_name": "STRAFFORD",
      "scraper_md_name": "NH STRAFFORD",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for STRAFFORD.md dataset",
        "noCaptchaDetection": true,
        "noCaptchaDetectionReason": "STRAFFORD.md does have a captcha v3 which we can bypass"
      }
    },
    {
      "state_code": "NJ",
      "county_name": "UNION-last-recorded",
      "scraper_md_name": "NJ UNION-last-recorded",
      "scraper_type": "bot-human",
      "config": {}
    },
    {
      "state_code": "NJ",
      "county_name": "UNION",
      "scraper_md_name": "NJ UNION",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for UNION.md dataset"
      }
    },
    {
      "state_code": "NV",
      "county_name": "DOUGLAS",
      "scraper_md_name": "NV DOUGLAS",
      "scraper_type": "bot-human",
      "config": {
        "_comment": "Configuration for DOUGLAS.md dataset"
      }
    },
    {
      "state_code": "NY",
      "county_name": "BRONX",
      "scraper_md_name": "NY DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n"
        }
      }
    },
    {
      "state_code": "NY",
      "county_name": "KINGS",
      "scraper_md_name": "NY DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n"
        }
      }
    },
    {
      "state_code": "NY",
      "county_name": "NEW_YORK",
      "scraper_md_name": "NY DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n"
        }
      }
    },
    {
      "state_code": "NY",
      "county_name": "QUEENS",
      "scraper_md_name": "NY DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset",
        "retries": {
          "block_lot": "[page][goto] https://a836-acris.nyc.gov/DS/DocumentSearch/PartyName\n[if query.isBusinessName==='true']\n[stagehand][act] click the `Business:` radio button\n[stagehand][act] type `${query.ownerLastName}` into the BUSINESS NAME field\n[else]\n[stagehand][act] type `${query.ownerLastName}` into the input field under the column header labeled `LAST`\n[stagehand][act] type `${query.ownerFirstName}` into the input field under the column header labeled `FIRST`\n[endif]\n[stagehand][act] select option that contains `${query.county}` in Select Borough/County dropdown\n[stagehand][act] select `DEEDS AND OTHER CONVEYANCES` in Select Document Class dropdown\n[stagehand][act] click Search\n[page][waitfor] 1000\n[stagehand][act] look at the search results table, sort the rows by the Recorded/Filed column to find the most recent entry where Block is `${query.block}` and Lot is `${query.lot}`, then click the IMG button in that row\n[stagehand][waitfornavigation] 3000\n[stagehand][observe] the document image has loaded\n[page][waitfor] 3000\n[stagehand][waitfordownload]\n[page][clickselector] img[src*=\"save.png\"][title=\"Save\"]\n[stagehand][observe] wait for save modal to appear\n[page][waitfor] 5000\n[stagehand][act] click the OK button in the Save dialog\n[stagehand][triggerdownload] ./downloads/${query.propertyId}/deed.pdf\n"
        }
      }
    },
    {
      "state_code": "NY",
      "county_name": "DUTCHESS",
      "scraper_md_name": "NY DUTCHESS",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DUTCHESS.md dataset"
      }
    },
    {
      "state_code": "NY",
      "county_name": "TOMPKINS",
      "scraper_md_name": "NY TOMPKINS",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for TOMPKINS.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "ASHLAND",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "AUGLAIZE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "BELMONT",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "BUTLER",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "CARROLL",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "CHAMPAIGN",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "CLARK",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "CLERMONT",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "COSHOCTON",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "FAYETTE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "FULTON",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "GREENE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "GUERNSEY",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "HANCOCK",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "HARDIN",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "HARRISON",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "HENRY",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "HIGHLAND",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "JEFFERSON",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "MONROE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "MORGAN",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "OTTAWA",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "PICKAWAY",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "PIKE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "PREBLE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "RICHLAND",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "ROSS",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "SANDUSKY",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "SENECA",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "SHELBY",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "STARK",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "TUSCARAWAS",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "VAN_WERT",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "WASHINGTON",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "WAYNE",
      "scraper_md_name": "OH DEFAULT",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for DEFAULT.md dataset"
      }
    },
    {
      "state_code": "OH",
      "county_name": "FRANKLIN-apn",
      "scraper_md_name": "OH FRANKLIN-apn",
      "scraper_type": "bot-human",
      "config": {}
    },
    {
      "state_code": "OH",
      "county_name": "FRANKLIN",
      "scraper_md_name": "OH FRANKLIN",
      "scraper_type": "bot",
      "config": {
        "_comment": "Configuration for FRANKLIN.md dataset"
      }
    },
    {
      "state_code": "TST",
      "county_name": "TEST",
      "scraper_md_name": "TST TEST",
      "scraper_type": "bot-human",
      "config": {}
    }
  ]
}